/** The name shown inside the app. The home-screen name lives in app.json and
 *  only changes on a rebuild; this one ships over the air, so the two are kept
 *  deliberately separate. */
export const APP_NAME = 'Week One';
export const APP_BY = 'David Earls';
